#!/bin/bash
set -euo pipefail

if [ "${1-}" = install ]; then
  printf '%s\n' 'bun-install' >>"$INTEGRATION_LOG"
  exit 0
fi

if [ "${1-}" = pm ] && [ "${2-}" = ls ] && [ "${3-}" = -g ]; then
  exit 0
fi

exec "$REAL_BUN" "$@"
